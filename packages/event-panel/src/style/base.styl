@import('./style.css')

* {
  box-sizing: border-box;
}

.icon-fj,
.icon-arrow
.icon-fx
.icon-arr
.icon-yj
  cursor: pointer
  &::before
    color: rgba(255, 255, 255, 0.9)
  &:hover
    &::before
      color: #fff
